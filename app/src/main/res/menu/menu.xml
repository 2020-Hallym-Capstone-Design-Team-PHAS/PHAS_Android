<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <group>
        <item
            android:id="@+id/appInfo"
            android:icon="@drawable/info_icon"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:title="어플리케이션 정보" />

        <item
            android:id="@+id/caution"
            android:icon="@drawable/using_icon"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:title="사용방법 및 주의사항" />
        <item
            android:id="@+id/dogRegister"
            android:icon="@drawable/drawer_dog_icon"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:title="강아지 등록" />
        <item
            android:id="@+id/dogList"
            android:icon="@drawable/dog_list_icon"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:title="강아지 목록" />

        <item
            android:id="@+id/logout"
            android:icon="@drawable/logout_icon"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:title="로그아웃" />
    </group>
</menu>